import * as React from 'react';
import {Text} from 'react-native';
import {PageContainer} from '../components';

const HelloWorldApp = () => {
  return (
    <PageContainer>
      <Text>THis is the HELLO WORLD APP</Text>
    </PageContainer>
  );
};

export default HelloWorldApp;
